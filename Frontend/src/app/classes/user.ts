export class User {
  constructor (public skills : string[], public id : string, public email : string, public name: string, public avatar : string, public bio : string, public location : string) {}
}
